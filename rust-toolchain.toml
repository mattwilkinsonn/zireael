[toolchain]
# Exact pin (not "stable") so devenv/fenix + CI share one derivation and caches
# survive. fenix reads this file; bump the sha256 in devenv.nix on version bumps.
channel = "1.96.0"
components = ["rustfmt", "clippy"]
