# yaml-language-server: $schema=https://moonrepo.dev/schemas/project.json
#
# gh-route — a bun/TypeScript CLI with its own bun.lock (not a hoisted root
# workspace), so it owns its `install`. System tasks exec the bun toolchain the
# devenv shell provides via proto: biome check + tsc --noEmit + bun test.
layer: 'application'
language: 'typescript'

tasks:
  install:
    command: 'bun install --frozen-lockfile'
    inputs: ['package.json', 'bun.lock']
    options:
      cache: false
  lint:
    command: 'bunx biome check .'
    deps: ['install']
    inputs: ['*.ts', 'biome.json', 'package.json']
  typecheck:
    command: 'bunx tsc --noEmit'
    deps: ['install']
    inputs: ['*.ts', 'tsconfig.json', 'package.json']
  test:
    command: 'bun test'
    deps: ['install']
    inputs: ['*.ts', 'package.json', 'bun.lock']
    options:
      cache: false
  ci:
    deps: ['lint', 'typecheck', 'test']
    options:
      cache: false
